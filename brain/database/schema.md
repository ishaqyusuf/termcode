# Schema

## Purpose
Track persistent local data formats.

## How To Use
- Update when config files change.
- Treat these formats as user-facing compatibility contracts.

## Template
- `roots`: newline-separated absolute paths.
- `aliases`: tab-separated `alias<TAB>absolute_project_path`.
